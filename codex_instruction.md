### Implementation Instructions for Codex

1. **Create a New File**:
   - Navigate to the root directory of the repository.
   - Create a new file named `ai_factory_pr_test.txt`.

2. **Write Content to the File**:
   - Open `ai_factory_pr_test.txt`.
   - Insert the following line into the file:
     ```
     ALTANA AI FACTORY PR TEST
     ```

3. **Save the File**:
   - Ensure the file is saved properly in the repository.