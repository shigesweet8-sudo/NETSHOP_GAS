### Implementation Instructions for Codex

1. **Create a New File:**
   - File name: `ai_factory_test.txt`
   - Location: Repository root

2. **Write Content to the File:**
   - Insert the following line into `ai_factory_test.txt`:
     ```
     ALTANA AI FACTORY SUCCESS
     ```

3. **Save the File:**  
   Ensure the file is saved in the repository with the specified content.