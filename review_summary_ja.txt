ALTANA AI FACTORY レビュー

Issue
修正③

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [WARNING] 20_api_netshop.gs:44
  - 内容: 新規トップレベル変数 PERSONAL_INFO_FIELDS を追加していますが、初期化で ITEM_HEADER_TO_FIELD を直接参照しています。
  - なぜ問題か／どこが問題か: この参照はスクリプトロード時（top-level）に実行されます。もし ITEM_HEADER_TO_FIELD が同ファイル内でまだ定義されていない、あるいは未定義であれば ReferenceError / TypeError になりスクリプト全体がロード不能になります。安全のため定義済みかどうかのガード、あるいは遅延初期化（関数内で計算）にするべきです。

- [WARNING] 20_api_netshop.gs:199（関数 buildPublicItemFromRow_ の戻り直前）
  - 内容: 以前は convertValueKeysToApi_(source) を返していましたが、今回の差分で convertValueKeysToApi_ の呼び出しを削除し、source をそのまま返すように変更しています。
  - なぜ問題か／どこが問題か: convertValueKeysToApi_ が値の型変換・正規化・ネスト変換など重要な加工をしている可能性が高く、これを取り除くと API の出力値（数値/文字列/真偽値の型や構造）が変わり、既存のクライアントや更新系処理との互換性が壊れる恐れがあります。キー名だけでなく"値の形式"の扱いが変わっていないか確認する必要があります。

- [WARNING] 20_api_netshop.gs:199（buildPublicItemFromRow_ 内のフィルタ）
  - 内容: header から toApiFieldKey_ を取得した後、Object.prototype.hasOwnProperty.call(ITEM_FIELD_TO_HEADER, field) の存在チェックで採用可否を決めています。
  - なぜ問題か／どこが問題か: (a) ITEM_FIELD_TO_HEADER が未定義だと hasOwnProperty.call(...) が例外を投げる可能性がある点、(b) toApiFieldKey_ の出力を ITEM_FIELD_TO_HEADER（フィールド→ヘッダの逆マップ）で検査する設計が意図通りか不明な点です。意図が「英字フィールドが既知のフィールド集合に含まれるか」を確認することなら構わないですが、マッピングの片方向／逆方向の不一致で正当な列を誤って除外するリスクがあります。変数存在チェックと、なぜ ITEM_FIELD_TO_HEADER を使うのかのコメントが必要です。

- [WARNING] 20_api_netshop.gs:199（個人情報除外判定の変更）
  - 内容: 個人情報除外判定を header（日本語ヘッダ）ベースから field（英字キー）ベースの PERSONAL_INFO_FIELDS を使うように変えています。
  - なぜ問題か／どこが問題か: マッピング整合性が取れていないと、これまで除外されていた列が誤って残ったり、逆に誤って除外される可能性があります。PERSONAL_INFO_FIELDS の生成が正しく ITEM_HEADER_TO_FIELD を使えているかを確認してください。

- [SAFE] codex_instruction.md / issue_body.md / openai_response.json
  - 内容: ドキュメント／メタ情報の更新。差分は説明文やメタ値の更新であり、実装コードとは独立。
  - なぜ問題か／どこが問題か: 単なるドキュメント更新であり問題は確認できません。ただし実装とドキュメントが一致しているか（特に getItem/listItems の振る舞い）を確認してください。

OPEN QUESTIONS:
- ITEM_HEADER_TO_FIELD と ITEM_FIELD_TO_HEADER（および convertValueKeysToApi_）はこのファイル内でどこに定義されていますか？トップレベルでの依存に問題がないことを確認できますか。（未定義ならこのパッチはロード時に失敗します）
- convertValueKeysToApi_ を呼ばなくなったのは意図的ですか？もし既存で値の正規化やネスト変換を担っているなら、取り除くことで互換性破壊が起きます。削除意図の根拠（例：不要になった／別の箇所で代替済み）を教えてください。
- ITEM_FIELD_TO_HEADER を使用してフィルタしている理由を明確にできますか？toApiFieldKey_ の出力を別マップで検証する設計意図（逆マップの整合確認なのか、存在チェックなのか）を確認してください。
- PERSONAL_INFO_FIELDS をトップレベルで静的に生成していますが、ITEM_HEADER_TO_FIELD の初期化タイミングに依存しないよう遅延生成（関数化）にすることは可能ですか？
- 既存の単体テスト / e2e テストで listItems/getItem の出力が英字キーかつ値フォーマットが期待通りであることを検証するテストはありますか？なければ追加を推奨します。

SUMMARY:
- 変更は「取得系 API の公開キーを日本語→英字にする」という目的に沿ったもので、buildPublicItemFromRow_ の出力を英字キーにしている点は意図通りに見えます。ただしトップレベルで既存マップ（ITEM_HEADER_TO_FIELD 等）を参照する実装と、以前の convertValueKeysToApi_ を削除した点が潜在的なロード失敗や値フォーマットの互換性破壊を招くリスクがあります。依存するマップの定義場所・初期化順と、値変換（convertValueKeysToApi_）の扱いを明確化し、該当箇所のガード・テストを追加したうえで再レビューしてください。
