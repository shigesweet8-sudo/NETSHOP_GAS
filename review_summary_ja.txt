ALTANA AI FACTORY レビュー

Issue
UI設計着手前の確１

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [WARNING] 20_api_netshop.gs:extractUiFilterPayload_ — ITEM_FIELD_TO_HEADER を直接参照しています (Object.keys(ITEM_FIELD_TO_HEADER)). この定数がこのファイル内またはグローバルで必ず定義されているとは diff からは確認できません。未定義だとこの関数呼び出し時に TypeError (Cannot convert undefined or null to object) が発生し、UI API が例外で失敗します。対処案：存在チェックを入れるか、フォールバック（空オブジェクト）を使用してください。
- [WARNING] 20_api_netshop.gs:api_uiDashboardKpi — getDashboardSummary を存在チェックせずに呼んでいます。try/catch により例外はハンドルされますが、存在しない場合のエラーメッセージが分かりにくくなります。関数未定義時は明示的なエラー応答を返すか、呼び出す前に typeof チェックを追加することを推奨します。
- [WARNING] 20_api_netshop.gs:api_uiExportCsv — CSV の戻り値として csvText（元文字列、BOM を含む可能性あり）を返しつつ、body（BOM を取り除いた文字列）を行数計算にのみ使用しています。UI に渡す CSV を BOM あり/なしどちらにするか仕様を明確にしてください。現状だと hasBom フラグは返されていますが、UI 側でどちらを使うべきか混乱する可能性があります。
- [WARNING] 20_api_netshop.gs:api_uiLookupZip — lookupAddressByZip の有無はチェックしていますが、lookupAddressByZip が非同期処理や例外を投げる実装だと catch 以外の細かい状況説明が無いため、UI 側で問題切り分けがしづらくなります。問題が発生したときにどのような details を含めるか検討してください（ただし個人情報取り扱いに注意）。
- [WARNING] 20_api_netshop.gs:命名/戻り値整合性 — buildUiApiSuccessResponse_ / buildUiApiErrorResponse_ による統一レスポンスは良いですが、既存の他 API から返ってくる生の配列や文字列をこのラッパーで吸収する前提があるかどうかは diff から未確認です。既存の API 呼び出し経路が配列直返し等を期待しているクライアントが無いか確認してください。

OPEN QUESTIONS:
- ITEM_FIELD_TO_HEADER はこのリポジトリ上で定義済みですか？（存在しない場合は extractUiFilterPayload_ が確実に失敗します）
- getDashboardSummary / exportCsv / lookupAddressByZip の定義場所と戻り値フォーマットはどのようになっていますか？（期待される構造に合わせたラップが行われていますか）
- api_uiExportCsv で UI に渡す CSV は BOM を残す想定ですか？それとも BOM を除去したものを渡す想定ですか？
- これらの UI 用 API を呼び出す既存のフロントエンド（または他のコード）がある場合、戻り値形式の変更（{ok,data} / {ok,false,error}）に影響する呼び出しはありませんか？

SUMMARY:
- 20_api_netshop.gs に UI 用の API 入口とレスポンス統一ヘルパーが追加されており、概ね意図どおりに実装されています。重大な構文エラーや即時の致命的不具合は見つかりませんでした。ただし、ITEM_FIELD_TO_HEADER の存在確認漏れと外部関数（getDashboardSummary/exportCsv 等）への依存が diff から未確認のため、呼び出し時のランタイム例外や期待フォーマットの不一致が発生する可能性があります。これらについて確認・防御コードの追加をお願いします。
