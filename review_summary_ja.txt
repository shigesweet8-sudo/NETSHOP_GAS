ALTANA AI FACTORY レビュー

Issue
修正②

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [WARNING] 20_api_netshop.gs:+44 関数 toApiFieldKey_ が key === null/undefined の場合に空文字列 ('') を返す実装になっています。convertValueKeysToApi_ / convertFilterKeysToApi_ でこの戻り値をオブジェクトのキーとして使うため、null/undefined のキーが存在すると空キー ('') が生成されてしまい、意図しないキー衝突や不正なレスポンスを生む可能性があります。null/undefined の扱いはスキップするか、呼び出し元で検証する方が安全です。
- [WARNING] 20_api_netshop.gs:+52 convertValueKeysToApi_ はネストした配列／オブジェクトを再帰的に変換しますが、convertFilterKeysToApi_（+86）はトップレベルのキーのみ変換しており、フィルタの値がネストオブジェクト（例: 条件式や演算子を含む構造）の場合に変換漏れが発生します。フィルタ仕様にネストがあるなら再帰変換が必要です。
- [WARNING] 20_api_netshop.gs:+191 buildPublicItemFromRow_ が作る source オブジェクトのキーを convertValueKeysToApi_ で一括変換していますが、行ヘッダ header の値が undefined/null のケース（ヘッダ行不整合や読み取りエラー）があると上記の空文字キー問題に繋がります。ヘッダの存在チェック（空文字や falsy のスキップ）を入れることを推奨します。
- [WARNING] 20_api_netshop.gs:+254 resolveSortKey_ において items[0] を参照して存在確認を行っています（既存挙動に合わせた実装）。items が空配列の場合は既存コード同様に問題がありますが、この差分で早期 return がなくなっている箇所があるため、空配列ガード（items.length===0）を入れると安全です。
- [WARNING] 20_api_netshop.gs:+769 / +975 / +1159 新たに validateVariation(), getMonthlySummary(), getPlatformSummary() といったラッパー関数が追加されています。既に同名関数が別箇所で定義されていると意図しない上書き（影響範囲の変更）になるため、既存定義の有無を確認してください。diff だけでは既存存在が確認できないため要確認です。
- [WARNING] codex_instruction.md / issue_body.md の変更で「対象 API の公開レスポンスを英字キーへ統一する」ことが明記されていますが、実コード差分はファイル全体の一部に留まっており（例: createItem/updateItem 等の戻り値生成箇所の変換が diff に見えない）、ドキュメントで列挙されている全 API が実際に英字キー化されているかは diff だけでは確認できません。未変換箇所が残っていると API スキーマの不整合を生みます。該当する各 API の返却箇所の差分を確認してください。

OPEN QUESTIONS:
- validateVariation(), getMonthlySummary(), getPlatformSummary() の新規ラッパーは既存の同名関数を置換する意図ですか？それとも外部公開用の別名ラッパーですか？（既存定義の有無を教えてください）
- フィルタ構造にネスト（例: { price: { $gte: 100 } }）や複雑な演算子が使われますか？使われる場合は convertFilterKeysToApi_ の再帰対応が必要ですか？
- ヘッダ読み取り時に header が null/undefined となるケース（シートの先頭行が期待と異なる等）に対する運用方針は「明示的に失敗（例外）」でしたか？現在の実装では部分的に空キーを生成するリスクがあります。
- API 全体で「英字キー化」を完了させるスコープはこの PR に完全に含まれていますか？（例: createItem/updateItem/recalculate... 等の全ての返却箇所が変換済みか確認したいです）

SUMMARY:
- 追加された変換ユーティリティ（toApiFieldKey_, convertValueKeysToApi_, convertFilterKeysToApi_）は API 側キー統一の方向性に沿っており概ね適切です。ただし、null/undefined キーの扱いやフィルタのネスト対応、既存関数名の衝突など運用上の穴が残っています。ドキュメントで列挙された全 API が実際に変換済みかを含め、上記の点を確認・修正してください。
