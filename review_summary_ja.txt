ALTANA AI FACTORY レビュー

Issue
feat: [11] 月別集計API を実装する

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [WARNING] 20_api_netshop.gs:+1..+20 (parseDateValue_)  
  新たに追加された parseDateValue_ は日付文字列を new Date(...) でパースしている点は許容できますが、GAS のロケール/文字列形式依存のため不正な文字列を null にしている点を明記する必要があります（仕様として許容する/しないを明示してください）。

- [WARNING] 20_api_netshop.gs:+21..+40 (parseAmountOrNull_)  
  金額パーサで空白・全角カンマ・円記号を除去する実装は良いですが、負数や括弧表記、千区切り以外のフォーマット（例えば小数点のロケール差）が仕様に含まれる場合に対応していません。対応要否を確認してください。

- [WARNING] 20_api_netshop.gs:+41..+56 (formatMonthKey_)  
  Utilities.formatDate を使って yyyy-MM を生成していますが、date 引数が null/undefined の場合のガードがないため、呼び出し側で確実に Date を渡すこと（またはここでの検証）を保証してください。

- [WARNING] 20_api_netshop.gs:+57..+126 (getMonthlySummaryRows_)  
  - 関数内で CONFIG、getManagementSheet_、CONFIG.COLS、CONFIG.CALC_TARGETS、calculateProfitFromRowValues_ を参照していますが、これらがこのファイル内に存在するか、意図どおりの定義（列インデックスや MEMO の値）であることは diff から確認できません。存在しない/定義が異なる場合は実行時エラーになります。  
  - sheet.getRange(2,1,lastRow-1, CONFIG.COLS.MEMO).getValues() で一括取得していますが、CONFIG.COLS.MEMO が参照する最大列（PRICE_FINAL, FEE, SHIPPING, COST, PROFIT 等）以上であることを保証してください。足りないと列データが抜けて誤集計になります。  
  - profit 集計ロジック: profit = parseAmountOrNull_(...) が null のとき calculateProfitFromRowValues_(row) を使いますが、その戻り値が数値である保証がありません。さらに entry.profit += profit としているため profit が null/undefined/NaN のままだと結果が NaN になるリスクがあります。数値化/デフォルト 0 のフォールバックを必ず行ってください。

- [WARNING] 20_api_netshop.gs:+92..+126 (公開API関数群: getMonthlySummaryForApi / api_getMonthlySummary / api_monthlySummary / api_dispatchAction)  
  - api_dispatchAction(payload) を新規に追加していますが、既存の API ルーティング関数（同名/同責務）がこのファイルまたは他ファイルに既に存在するかどうかは diff から不明です。既存のディスパッチ方式と衝突すると想定外の動作になります。既存の dispatcher に「monthlySummary」アクションを追加するべきか、新しいディスパッチ関数を定義する意図かを確認してください。  
  - api_getMonthlySummary は例外をキャッチして monthly: [] を返しており堅牢ですが、エラー時のログが Logger.log('api_getMonthlySummary error: ' + error) のみで詳細スタックを出していないため、運用時の原因追跡にやや不便です（必要なら error.stack などを追加検討）。

- [WARNING] codex_instruction.md / issue_body.md の変更  
  ドキュメント（codex_instruction.md / issue_body.md）は新API仕様に更新されていますが、実装側で参照している列名・列位置（D列 DATE、PRICE_FINAL 等）が本リポジトリの CONFIG と一致しているかは diff から確認できません。ドキュメントとコードの整合性チェックをお願いします。

OPEN QUESTIONS:
- calculateProfitFromRowValues_ はこのリポジトリ内で定義済みですか？（存在しない場合は getMonthlySummaryRows_ での利用は実行時エラーになります）
- getManagementSheet_ と CONFIG（特に CONFIG.COLS.MEMO、CONFIG.COLS.PRICE_FINAL / FEE / SHIPPING / COST / PROFIT、CONFIG.CALC_TARGETS）は既存定義で今回の使用に適合しますか？列数 (MEMO) が集計に必要な列数以上であることを確認してください。
- 既存の API ディスパッチ実装（どの関数が central dispatcher を担っているか）に api_dispatchAction を追加する想定でしょうか？既存 dispatcher が別名なら統合方法を教えてください。
- 利益算出の整合性要件：calculateProfitFromRowValues_ が存在しない/使わない場合、利益計算はどの列／式に優先して依存すべきですか？（ドキュメントでは「既存の粗利計算に合わせる」としていますが、今回の実装では fallback を使用しています）

SUMMARY:
- 月別集計APIの本体と複数のヘルパー関数が 20_api_netshop.gs に追加され、レスポンス/エラーハンドリングの枠組みも用意されています。  
- ただし、diff 上で参照されている外部シンボル（CONFIG、getManagementSheet_、calculateProfitFromRowValues_ 等）の存在・定義が確認できないため、実行時エラーや誤集計につながる可能性があります。また、api_dispatchAction の追加が既存のディスパッチと衝突する恐れがあります。  
- 上記点の確認と、profit の数値化ガード、CONFIG.COLS.MEMO と参照列の整合性確保、既存 dispatcher との統合を行ってください。問題が解消されればマージ可能です。
