ALTANA AI FACTORY レビュー

Issue
phase１最適化

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, issue_tasks.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [warning] 20_api_netshop.gs:1-120 共通レスポンスヘルパーの追加（buildApiSuccessResponse_, buildApiErrorResponse_ 等）は概ね正しいが、エラー詳細に「payload/result オブジェクト」をそのまま埋めるケースが複数あります。例えば bulkRecalculateProfit の catch/エラー経路（関数定義周辺）や buildDisableResponse_/buildArchiveResponse_（それぞれ payload を details/extra に渡す）で、内部の item オブジェクトやシート由来のフィールドが error.details や extra に含まれる可能性があります。シート内部のカラムや個人情報が含まれる場合、情報漏洩につながるリスクがあります。
- [warning] 20_api_netshop.gs:224-320 多くの既存関数を「Raw_」サフィックス付きの内部実装とし、元の名前を API ラッパー（ok/error/data を返す）に置き換えています（例: listItems → listItemsRaw_ / listItems（ラッパー））。このパターンは UI 入口向けには良いが、プロジェクト内の他のコード（別ファイル・トリガー・内部ユーティリティ等）が従来の生データ（配列や単純オブジェクト）を直接返すことを前提に listItems 等を呼んでいる場合、戻り値の型が変わるため互換性破壊になります。diff 上で全ての内部呼び出しが Raw_ を参照するように置換されているかは確認できないため、既存呼び出し側の影響調査が必要です。
- [warning] 20_api_netshop.gs:950-1010 validateVariation 系の変更では、元の validateVariation が API ラッパー（ok/error/data）を返すように置き換えられています。validateVariation は検証ユーティリティとして内部からも使われるケースが想定されやすく、内部利用箇所が生の検証オブジェクトを期待している場合は破壊的です（diff では呼び出し箇所の網羅確認ができないため要確認）。
- [warning] 20_api_netshop.gs:1317-1560 サマリー系・CSV 出力系 API をラップして ok/data 形式にした変更は UI には有用ですが、exportCsv 等の関数名を wrapper と raw の二重構造にしているため、他モジュールが exportCsv を直接呼んで CSV 文字列を期待している場合に戻り値がオブジェクトに変わります。diff 上で呼び出し元の全置換が確認できないため影響調査が必要です。
- [warning] 20_api_netshop.gs:1854-2060 buildBulkDisableResponse_, buildArchiveResponse_ 等で成功時・失敗時に返す shape が以前とは変わっています（ラッパーで ok/error/data を返す）。API の互換性維持のためトップレベルへの互換フィールド（例: items, platforms 等）を浅くコピーする意図はあるものの、copyDataToTopLevel オプションの利用でトップレベルに冗長なフィールドが増える可能性があります。既存の呼び出し側がトップレベルフィールドに依存している場合、どのキーを正として参照すべきかのドキュメント化が必要です。
- [warning] 20_api_netshop.gs:1420-1500 buildUiApiErrorResponse_ のシグネチャを (message, details, code) に変更し buildApiErrorResponse_ を呼ぶ形に変更されています。diff 内の呼び出しは概ね合わせて更新されていますが、他ファイルからの呼び出しや未更新の呼び出しが残っていると引数の順序違いで誤った code/details の割当が起き得ます（呼び出し側の網羅確認を推奨）。
- [warning] 20_api_netshop.gs:2018-2362 updateItemStatus / recalculateItemProfit 等で「Raw_」実装と API ラッパーを二重に定義していますが、同名の api_* エンドポイントが別に存在するところと混在しています。diff 上は多くの api_* が新しい wrapper を経由するように見えますが、古い呼び出し経路（外部スクリプトやトリガー）が残る可能性があるため、外部参照の洗い出しが必要です。

OPEN QUESTIONS:
- このリポジトリ内で別ファイル（例: 10_webapp.gs, 02_triggers.gs など）や外部のバッチスクリプトが、今回ラップされた関数（listItems, getItem, exportCsv, validateVariation, createItem, updateItem, updateItemStatus 等）を「生の戻り値」を期待して直接呼んでいる箇所はありますか？ある場合、呼び出し側の差し替え/テストは済んでいますか？
- error.details や compatibility.extra として返しているオブジェクト（payload/result）にシートの内部カラム（個人情報や運用上公開すべきでない値）が含まれる可能性はありますか？もしある場合、どのフィールドを除外するポリシーにしますか（例: 個人情報は除外）？
- UI 側や既存の api_dispatchAction を経由するクライアントが、トップレベルの移行的フィールド（例: items, item, platforms, monthly, masters 等）に依存している件数は確認できますか？依存が多い場合、互換維持のための追加ラッパーが必要です。
- buildApiErrorResponse_ の第3引数(details) と第4引数(compatibility) の使い分けルール（何を details に入れるか、compat.extra にどれだけ互換フィールドを残すか）をチームで合意していますか？（必要ならガイドライン化を推奨します）

SUMMARY:
- 目的どおり UI 向けに成功/失敗レスポンスを統一するヘルパー追加と各 API のラッピングは概ね実装されています。コードの一貫性向上と UI 側の扱いやすさは改善される見込みです。
- ただし、既存の内部/外部呼び出しが「生の戻り値（配列・文字列・オブジェクトなど）」を期待している場合、戻り値型の変更により互換性が壊れるリスクがあります（diff 上で網羅的に置換されているか不明）。また、エラー時に内部オブジェクトを details/extra に詰める実装が複数あり、シート内部データ（場合によっては個人情報）を外部へ露出する可能性があるため注意が必要です。
- 提案: ① 既存呼び出し（別ファイル・外部スクリプト含む）をリストアップして Raw_/wrapper 置換の影響を確認する、② エラーの details/extra に含めるフィールドを最小限にしセンシティブ情報を除外するポリシーを適用する、③ 変更後の戻り値仕様を簡潔にドキュメント化して依存箇所の修正を確実に行ってください。
