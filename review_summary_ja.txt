ALTANA AI FACTORY レビュー

Issue
APIチューニング完了

変更ファイル
01_config.gs, 20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [SAFE] 01_config.gs:107-123 追加された CONFIG.ARCHIVE 定数は妥当に見えます（MEMO_PREFIX と HEADER_CANDIDATES を追加）。CONFIG の不変化（Object.freeze）も維持されています。
- [WARNING] 20_api_netshop.gs:1046-1068 新規 ARCHIVE_HEADER_CANDIDATES / ARCHIVE_MEMO_PREFIX の定義は CONFIG から取得する実装になっていますが、既存の CONFIG 構造と確実に一致するか確認してください（CONFIG.ARCHIVE が常に存在する前提がない点は typeof チェックで回避されていますが、運用ルール上の候補語一覧の網羅性は運用担当と確認が必要です）。
- [WARNING] 20_api_netshop.gs:1077-1085 resolveArchiveHeader_ の実装は単純で正しく見えますが、headerResolution.indexByHeader のキー名（ヘッダ文字列）が他部分と完全一致するか（全角/半角／表記揺れ）を確認してください。ここが一致しないと archive を列に反映できません。
- [WARNING] 20_api_netshop.gs:1103-1118 appendArchiveMemo_ は既存メモと重複しないようPREFIX を付ける実装です。ただし現在のロジックだと「既存メモが空」かつ「PREFIX を含まない」場合にも PREFIX を追加します（意図的な挙動か確認ください）。またメモの挿入順（既存メモ → PREFIX → 理由）を意図どおりか確認してください。
- [WARNING] 20_api_netshop.gs:1135-1162 applyArchiveToRowValues_ は、該当ヘッダ列が見つかればその列に true を書き込む実装です。該当カラムがチェックボックス列／論理値を期待している前提になっているため、シート上の列型が文字列や別型の場合に値が不適切になる可能性があります。対象列型の事前確認か変換処理（'TRUE'/'FALSE' 等）の整合性確認を推奨します。
- [WARNING] 20_api_netshop.gs:1740-1754 buildArchiveResponse_ の operation フィールドに 'archiveStorage' が使われていますが、既存 API の operation 命名規則（disable 系や cancel 系の operation 名）と一貫しているか確認してください。命名が既存クライアントやログ集計に影響を与える可能性があります。
- [WARNING] 20_api_netshop.gs:1798-1823 archiveItemById の実装は disableItemById の単純な委譲をやめ、直接シート更新する実装に変わっています。大きな変更点としては snapshot を更新してから snapshot.sheet に setValues() している点です。getItem(normalizedId) を返していますが、getItem がシートから最新の値を再取得する実装であるか（snapshot の書き込み反映後に取得されるか）を確認してください。getItem が snapshot を参照する実装だと古い値が返る可能性があります。
- [WARNING] 20_api_netshop.gs:1798-1823 archiveItemById で使用している行オフセット計算（getRange(rowIndex + 2, 1, 1, ...））は他の更新処理と同じオフセット設計か確認してください（ヘッダ行数が異なるとオフセットずれで別行を書き換えるリスクがあります）。
- [WARNING] 20_api_netshop.gs:1979-1998 api_archiveItem の実装は try/catch で安全にエラーを返していますが、失敗時の返却 itemId を空文字にしているため、呼び出し元で itemId を参照してエラーハンドリングする実装がある場合は取り扱いに注意が必要です（エラー時に元の itemId を返すほうが診断しやすいケースがあります）。
- [WARNING] 複数ファイル（codex_instruction.md / issue_body.md / openai_response.json）で仕様文言の更新がされていますが、テスト・呼び出し側クライアントの期待値（action / operation / フィールド名）との整合性テストが必要です。spec 文書は更新済みですが、実装側の命名（operation: 'archiveStorage' 等）が spec と完全に一致しているか確認してください。

OPEN QUESTIONS:
- getItem(normalizedId) はシートを再読み込みして最新の行を返しますか？それとも snapshot 等のキャッシュを参照しますか？（未確認のため、archiveItemById の戻り値が更新後の値を返すか確認が必要）
- ITEM_FIELD_TO_HEADER.memo（ヘッダ名マッピング）は常に headerResolution に含まれる想定でしょうか？見つからないケースの取り扱い（memoIndex が undefined の場合の振る舞い）が要確認です。
- 対象の「アーカイブ」操作は列に boolean を入れる想定（チェックボックス）ですか？文字列やステータスコードを入れる想定ですか？（列型の期待値に依存します）
- buildArchiveResponse_ の operation 名 'archiveStorage' は既存のログ・モニタリングやクライアント期待と一致しますか？命名規則に違いがある場合は統一が必要です。
- archive 用の HEADER_CANDIDATES の語彙（'保管' 等）は運用上十分でしょうか。ローカル表記ゆれ（全角スペースや別表記）がある場合の検出漏れがないか確認してください。

SUMMARY:
- コード差分は「archive」を disable から明確に分離する変更を意図どおりに実装しており、CONFIG 追加・ヘッダ候補の分離・専用の更新/レスポンス関数の導入が行われています。構文上の致命的なエラーは見つかりませんでした。
- ただし、シート列型（チェックボックス vs 文字列）やヘッダ名の表記揺れ、getItem の取得方法、operation 名の一貫性など運用上・互換性上の確認ポイントが残っています。これらを確認・テストして問題がなければマージしてよいと判断します。
