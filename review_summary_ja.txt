ALTANA AI FACTORY レビュー

Issue
[14] 並び替えAPI を実装対象とします。

変更ファイル
20_api_netshop.gs, codex_instruction.md, issue_body.md, openai_response.json

実装判定
実装あり

マージ判定
マージOK

NG種別
OK

判定理由
実装ファイル差分があり、重大な指摘は検出されませんでした。

レビュー結果
SEVERITY: WARNING
ACTION: REVIEW NEEDED

FINDINGS:
- [WARNING] 20_api_netshop.gs:122-311 関数群（normalizeSortOrder_, resolveSortKey_, normalizeComparableValue_, compareComparableValues_, sortItemsByKey_, listItemsSorted, api_listItemsSorted）が追加されていますが、コード内で参照している定数 ITEM_FIELD_TO_HEADER の定義がこの差分内に見えません。もしこの定数が別ファイルに存在しない場合、resolveSortKey_ の実行時に ReferenceError が発生して API が失敗します。定義の所在（ファイル/スコープ）を確認してください。
- [WARNING] 20_api_netshop.gs:122-311 resolveSortKey_ はキー存在判定に items[0] のみを用いています。配列の先頭要素にそのキーが無いケース（先頭だけ欠損で他要素に存在する等）では誤ってキー未存在と判断され、ソートが行われない可能性があります。意図的であればコメント付与、そうでなければ全要素またはヘッダー情報で判定する方が堅牢です。
- [WARNING] 20_api_netshop.gs:122-311 compareComparableValues_ は異なる型間（例: 数値と文字列）の比較で left.raw || left.value を文字列化して lexicographical 比較にフォールバックしています。これにより「2」「10」のような数値を文字列として比較した場合に期待と違う順序になる可能性があります（例: '10' < '2' のような挙動）。型混在データでのソート仕様を明確にするか、型優先ルールを実装してください。
- [WARNING] 20_api_netshop.gs:122-311 normalizeComparableValue_ の実装では boolean を数値 (1/0) として扱っています。仕様上これが期待された挙動か確認してください（true/false を文字列順で扱うことを期待している場合は不整合になります）。
- [WARNING] 20_api_netshop.gs:122-311 api_listItemsSorted の single-argument payload 判定ロジックに 'action' を含めています（arguments.length === 1 && payload has 'action' で分岐）。呼び出し側で action を含むオブジェクトを渡すケースがあると期待通りに single-payload パースが走るため意図外の分岐が発生する可能性があります。他の API と挙動を合わせるか、判定条件を絞ってください。
- [SAFE] 20_api_netshop.gs:122-311 全体としてはソート処理は「安定ソート（元インデックス保持）」や昇降指定の正規化（normalizeSortOrder_）など、ドキュメント要件（シートは変更せず返却配列の順序のみを変更、未指定時は既存順維持）に沿う形で実装されています。重大な構文エラーは見当たりません。
- [WARNING] 20_api_netshop.gs:1135-1140 api_dispatchAction に 'listItemsSorted' / 'sortedItems' / 'getSortedItems' の新規ルーティングが追加されています。ルーティング名自体は問題ありませんが、既存の API 呼び出し側（ドキュメント/クライアント）との整合性（アクション名の命名や期待されるレスポンス形式）が保たれているか確認してください。既存の他ルート実装と同様に、返却型（配列そのまま or {ok:..., items:...}）の期待が一致しているかを確かめる必要があります。
- [WARNING] codex_instruction.md / issue_body.md / openai_response.json も更新されていますが、これらはドキュメント変更であり実装に紐づく想定変更点の説明は追加されています。実装側（コード）がドキュメント通りに動作するかの単体/統合テストが不足している可能性があります。

OPEN QUESTIONS:
- ITEM_FIELD_TO_HEADER はこのプロジェクト内で定義済みですか？定義場所（ファイル）を教えてください。未定義なら定義を追加する必要があります。
- listItems(filter) の返却形式は確実に「配列 of オブジェクト」ですか？（api_listItemsSorted はそれを前提に動作します）
- API の既存利用者が期待するレスポンス形式（生の配列を返す vs { ok: true, items: [...] } のようなラッピング）はどちらですか？新ルーティングは既存形式に完全互換ですか？
- 異なる型（数値/文字列/日付/空値）が混在する列に対して、期待するソート仕様は何ですか？現在は「empty を末尾に、同値は安定ソート、型不一致は raw/value の文字列比較にフォールバック」になっていますが、これで問題ありませんか？
- api_listItemsSorted の single-payload 判定に 'action' を含めた意図は何ですか？他の api_* 関数と同様の慣習ですか？

SUMMARY:
- 並び替え機能（メモリ上で安定ソートする実装）と API 入口の追加は差分として適切に入っています。構文的な致命的問題は見つかりませんでした。ただし、ITEM_FIELD_TO_HEADER の参照可否、キー存在判定の堅牢性、異型データ間の比較ロジックなど、動作上の境界ケースで期待と異なる振る舞いをする可能性があります。上記の点を確認・修正し、既存 API 仕様との整合性テストを実施してからマージしてください。
